## Local .env example — DO NOT COMMIT REAL KEYS
# Set one key (single) or many keys (comma-separated) for rotation/failover.
# Example (do not copy real keys into source control):
# GROQ_API_KEY=gsk_your_single_key_here
# GROQ_API_KEYS=gsk_key_one,gsk_key_two,gsk_key_three

GROQ_API_KEY=
GROQ_API_KEYS=
GROQ_MODEL=qwen/qwen3-32b
LOG_LEVEL=INFO
MAX_PDF_SIZE_MB=25
OCR_MAX_PAGES=10
AI_RETRY_COUNT=3
AI_TIMEOUT_SECONDS=45
# Example environment file for the Universal Bank Statement Parser
# Copy this to `.env` and fill secrets/values. Do NOT commit a real `.env` to source control.

# API / Secrets
# Set your GROQ API key here (do NOT commit the real key)
GROQ_API_KEY=

# Model configuration
# Default model recommended for Groq's compound endpoint
GROQ_MODEL=groq/compound
# Optional: override the Groq API URL (default is the official endpoint)
GROQ_URL=https://api.groq.com/openai/v1/chat/completions

# Application configuration
LOG_LEVEL=INFO
MAX_PDF_SIZE_MB=25
OCR_MAX_PAGES=10
AI_RETRY_COUNT=3
AI_TIMEOUT_SECONDS=45




----------

# Local .env (placeholder)
# WARNING: Keep secrets out of source control. This file is intentionally
# added as a placeholder. In production, set environment variables via
# your deployment platform (Render, Docker secrets, etc.).

GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=meta-llama/llama-4-scout-17b-16e-instruct
LOG_LEVEL=INFO
MAX_PDF_SIZE_MB=50
OCR_MAX_PAGES=200
AI_RETRY_COUNT=1
AI_TIMEOUT_SECONDS=45
